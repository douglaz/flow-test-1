# flow-test-1

A minimal project containing a single text file.

## Contents

- **`hello.txt`** — Contains the text `Hello, World!` (single line, trailing newline). This is a simple static content file with no dependencies.

## Usage

To verify the file contents:

```sh
cat hello.txt
```

Expected output:

```
Hello, World!
```
