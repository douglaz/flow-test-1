---
artifact: impl-notes
loop: 3
project: issue-21
backend: claude(opus)
role: implementer
created_at: 2026-03-21T03:06:34Z
---

# Implementation Notes

## Decisions Made
- Wrapped `input()` in a try/except block catching `EOFError`, defaulting to an empty string. This is the minimal, idiomatic Python approach to handle genuine EOF on stdin without changing any other behavior.

## Spec Deviations
- None. All acceptance criteria are met exactly as specified.

## Testing
- `printf '' | python3 reverse.py` → prints empty line, exits 0
- `python3 reverse.py < /dev/null` → prints empty line, exits 0
- `echo "hello" | python3 reverse.py` → `olleh`
- `echo "" | python3 reverse.py` → empty line
- `echo "a" | python3 reverse.py` → `a`
- `echo "racecar" | python3 reverse.py` → `racecar`
- `echo "hello world" | python3 reverse.py` → `dlrow olleh`
