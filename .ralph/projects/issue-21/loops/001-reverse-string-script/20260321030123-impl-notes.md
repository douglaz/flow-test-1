---
artifact: impl-notes
loop: 1
project: issue-21
backend: claude(opus)
role: implementer
created_at: 2026-03-21T03:01:23Z
---

# Implementation Notes

## Decisions Made
- Used `input()` which automatically strips the trailing newline from stdin, satisfying the requirement to not reverse the newline character.
- Kept the script to two lines of procedural code — no imports, no functions, no error handling beyond what `input()` and `print()` provide natively.

## Spec Deviations
- None. All acceptance criteria are met exactly as specified.

## Testing
- `echo "hello" | python3 reverse.py` → `olleh`
- `echo "" | python3 reverse.py` → (empty line)
- `echo "a" | python3 reverse.py` → `a`
- `echo "racecar" | python3 reverse.py` → `racecar`
- `echo "hello world" | python3 reverse.py` → `dlrow olleh`
