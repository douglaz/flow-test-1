---
artifact: review-approved
loop: 1
iterations: 0
project: issue-21
backend: claude(opus)
role: reviewer
created_at: 2026-03-21T03:01:54Z
---

# Review: APPROVED

## Acceptance Criteria Checklist
- [x] `reverse.py` exists at the project root — verified at `/reverse.py` (2 lines, 33 bytes)
- [x] `echo "hello" | python reverse.py` outputs `olleh` — verified
- [x] Handles empty input by printing an empty line — verified (`echo "" | python3 reverse.py` produces empty output)
- [x] Strips the trailing newline from input before reversing — `input()` handles this natively; no newline character appears in reversed output
- [x] Single character input returns that character unchanged — `echo "a"` → `a`
- [x] Palindrome input returns the same string — `echo "racecar"` → `racecar`
- [x] Input with spaces reverses the entire string including spaces — `echo "hello world"` → `dlrow olleh`

## Notes
Implementation is minimal and correct: two lines of procedural Python using only `input()` and `print()` with `[::-1]` slice notation, exactly as specified. No imports, no dependencies, no over-engineering. `input()` naturally strips the trailing newline, satisfying that requirement without explicit handling.

## Commit Message
feat(issue-21): add reverse.py script that reads stdin and prints reversed string
