---
artifact: spec
loop: 1
project: issue-5
backend: claude(opus)
role: planner
created_at: 2026-03-18T22:29:23Z
---

The repository has only `README.md` and `.ralph/` — no `greet.py` exists yet. This is loop 1, planning phase, with no previous specs or completed loops. The task is straightforward: create `greet.py`.

# Feature: Implement greet.py CLI script

## Description
Create a Python 3 CLI script `greet.py` at the repository root that greets a user by name. The script reads the first positional argument via `sys.argv` and prints `Hello, {name}!` to stdout. If no argument is provided, it defaults to `"World"`.

## Acceptance Criteria
- [ ] `greet.py` exists at the repository root
- [ ] Running `python greet.py Alice` writes `Hello, Alice!` to stdout
- [ ] Running `python greet.py` writes `Hello, World!` to stdout
- [ ] Running `python greet.py "Bob Smith"` writes `Hello, Bob Smith!` to stdout
- [ ] The script uses `sys.argv` for argument parsing (no `argparse`, `click`, etc.)
- [ ] The script contains no imports other than `sys`
- [ ] No files other than `greet.py` are created or modified

## Files to Modify/Create
- `greet.py` (create) - New CLI script: import `sys`, check `len(sys.argv) > 1` to pick `sys.argv[1]` or `"World"`, then `print(f"Hello, {name}!")`

## Dependencies
- Requires: none
- Blocks: none (this is the only and final feature for the project)

---
