---
artifact: spec
loop: 1
project: issue-1
backend: claude(opus)
role: planner
created_at: 2026-03-18T21:37:26Z
---

`hello.py` does not exist yet. This is a single, atomic feature — no decomposition needed.

# Feature: Create hello.py

## Description
Create a new file `hello.py` at the repository root containing exactly one line — `print("Hello, World!")` — followed by a single trailing newline. No shebang, comments, imports, functions, classes, or guards. The script prints `Hello, World!` to stdout and exits with status 0.

## Acceptance Criteria
- [ ] `./hello.py` exists at the repository root
- [ ] `./hello.py` is byte-for-byte equal to `print("Hello, World!")\n` (29 bytes)
- [ ] Running `python hello.py` exits with status code `0`
- [ ] Running `python hello.py` writes exactly `Hello, World!\n` to stdout
- [ ] Running `python hello.py` writes nothing to stderr
- [ ] No other files are created, modified, renamed, or deleted

## Files to Modify/Create
- `hello.py` - Create new file with single line: `print("Hello, World!")`

## Dependencies
- Requires: none
- Blocks: none

---
