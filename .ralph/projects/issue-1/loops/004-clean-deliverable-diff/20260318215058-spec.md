---
artifact: spec
loop: 4
project: issue-1
backend: codex(gpt-5.4-xhigh)
role: planner
created_at: 2026-03-18T21:50:58Z
---

I’m checking the prompt, loop artifacts, and branch history to determine whether anything remains unmet or whether this should close as complete.
I’ve confirmed loop 1 implemented the script and the only live worktree delta is this planner log. Next I’m reading the prompt and completion/QA artifacts to decide whether the remaining gap is real product work or only workflow metadata.
The prompt itself is satisfied at the product level by `hello.py`; the only disagreement is whether `.ralph` workflow artifacts count as forbidden repo changes. I’m checking the actual diff and file content directly before deciding whether that warrants a new feature spec or a completion request.
I found the only possible remaining work: branch hygiene around `.ralph` artifacts. I’m checking whether the project’s own planning metadata explicitly scopes those out before I decide between a cleanup feature and a completion request.
# Feature: Clean Deliverable Diff

## Description
Loop 1 already completed the only product feature in the prompt: [`hello.py`](/tmp/ralph-daemon-test/douglaz/flow-test-1/.ralph/daemon/worktrees/douglaz-flow-test-1-1/hello.py) exists and later completion artifacts confirm its content and runtime behavior are correct. The remaining unmet work is repository-scope cleanup: git checkpoint history after loop 1 added many `.ralph` planning, QA, and log artifacts, so the branch still violates the prompt requirement that `hello.py` be the only added or modified deliverable file. This feature removes or excludes those workflow artifacts from the final handoff while preserving the existing `hello.py` bytes and behavior.

## Acceptance Criteria
- [ ] Relative to `master`, `git diff --name-only master...HEAD` lists only `hello.py`
- [ ] No tracked `.ralph/` path remains in the deliverable diff, and no untracked `.ralph/tmp/logs/*` files remain in the worktree at handoff
- [ ] [`hello.py`](/tmp/ralph-daemon-test/douglaz/flow-test-1/.ralph/daemon/worktrees/douglaz-flow-test-1-1/hello.py) remains byte-for-byte equal to `print("Hello, World!")\n`
- [ ] Running `python hello.py` from the repository root exits with status `0`, writes exactly `Hello, World!\n` to stdout, and writes nothing to stderr after cleanup
- [ ] If tracked artifact cleanup fails at any step, `hello.py` must remain unchanged and the branch must not be treated as acceptance-ready
- [ ] If tracked cleanup succeeds but untracked artifact removal fails, completion must still be blocked until the remaining `.ralph` paths are removed and the worktree is clean

## Files to Modify/Create
- [`.ralph/projects/issue-1`](/tmp/ralph-daemon-test/douglaz/flow-test-1/.ralph/daemon/worktrees/douglaz-flow-test-1-1/.ralph/projects/issue-1) - remove committed prompt, loop, and completion artifacts from the deliverable diff or otherwise exclude them from final handoff
- [`.ralph/quick-prd/c0b0386c37b1`](/tmp/ralph-daemon-test/douglaz/flow-test-1/.ralph/daemon/worktrees/douglaz-flow-test-1-1/.ralph/quick-prd/c0b0386c37b1) - remove generated PRD artifacts from the deliverable diff
- [`.ralph/tmp/logs`](/tmp/ralph-daemon-test/douglaz/flow-test-1/.ralph/daemon/worktrees/douglaz-flow-test-1-1/.ralph/tmp/logs) - remove generated logs and ensure no untracked workflow files remain at approval time
- [`hello.py`](/tmp/ralph-daemon-test/douglaz/flow-test-1/.ralph/daemon/worktrees/douglaz-flow-test-1-1/hello.py) - preserve existing exact content while cleanup occurs

## Dependencies
- Requires: Create hello.py
- Blocks: none

---
